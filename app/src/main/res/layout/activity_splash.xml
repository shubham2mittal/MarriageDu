<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SplashActivity">
<ImageView
    android:id="@+id/image"
    android:layout_width="300dp"
    android:layout_height="200dp"
    android:layout_centerInParent="true"
    android:background="@drawable/wedding"
    />
    <TextView
        android:layout_below="@+id/image"
        android:paddingTop="15dp"
        android:textAlignment="center"
        android:textColor="#000"
         android:textStyle="italic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Marriage is a union that binds two people as one and is thus an institution that is built on respect, loyalty, understanding and the willingness to hold on even in throes of challenges"
        android:gravity="center_horizontal" />

</RelativeLayout>